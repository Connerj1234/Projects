# Required for SEC EDGAR access policy compliance.
# Format example: Jane Doe jane@example.com
SEC_USER_AGENT=Conner Jamison mailtoclyjams@gmail.com

# Optional local fallback if SEC filings do not yield transcript-like text (CSV or Parquet).
# Required columns: ticker,date,year,quarter,content
TRANSCRIPTS_FALLBACK_PATH=

# Optional SEC request pacing (seconds between requests).
SEC_REQUEST_INTERVAL_SEC=0.25
